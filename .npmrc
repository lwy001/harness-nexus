# Harness Nexus-wide npm/pnpm configuration
# Hoist workspace deps into a discoverable node_modules for tools that need it
shamefully-hoist=false
auto-install-peers=true
strict-peer-dependencies=false
# Keep workspace links deterministic
link-workspace-packages=true
prefer-workspace-packages=true
