/**
 * Machine detail page — thin re-export; the implementation lives in
 * `machine-detail/` (page shell + one file per tab). See `machine-detail/index.tsx`.
 */
export { MachineDetailPage } from './machine-detail/index.js';
