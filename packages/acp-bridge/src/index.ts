#!/usr/bin/env node
/**
 * Harness Nexus ACP bridge — roadmap item.
 *
 * A local daemon that connects an ACP-capable coding tool (Claude Code, ZCode,
 * …) on this machine to a remote Harness Nexus server, enabling:
 *   - remote profile/resource pushes from the server to the tool
 *   - forwarding the tool's MCP traffic through the Harness Nexus proxy
 *
 * Skeleton only; not wired. See docs/roadmap.md.
 */
// eslint-disable-next-line no-console
console.log('harness-nexus-bridge — skeleton. ACP bridging is a roadmap item. (TODO)');
