#!/usr/bin/env node
/**
 * AgentNexus ACP bridge — roadmap item.
 *
 * A local daemon that connects an ACP-capable coding tool (Claude Code, ZCode,
 * …) on this machine to a remote AgentNexus server, enabling:
 *   - remote profile/resource pushes from the server to the tool
 *   - forwarding the tool's MCP traffic through the AgentNexus proxy
 *
 * Skeleton only; not wired. See docs/roadmap.md.
 */
// eslint-disable-next-line no-console
console.log('agent-nexus-bridge — skeleton. ACP bridging is a roadmap item. (TODO)');
