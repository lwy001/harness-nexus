{
  "name": "@agent-nexus/acp-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Local daemon bridging ACP-capable Agent tools to AgentNexus for remote management.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "agent-nexus-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint placeholder) add eslint/oxlint config\"",
    "test": "echo \"(test placeholder) add vitest\""
  },
  "dependencies": {
    "@agent-nexus/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.8.6",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
